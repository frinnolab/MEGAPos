@model MEGAPos.Models.Sales_Header
@{
    ViewBag.Title = "Delete Sale";
}


<h3>Are you sure you want to delete this?</h3>
<div>
    <h2>@ViewBag.Title</h2>

    <hr />
    @using (Html.BeginForm(null, null, new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "ui form root form-white form-center", role = "form" }))
    {

        <div class="registers">
            <div class="ui buttons ">
                <a class="ui button" href="#">@Model.Sale_Date Sales.</a>
            </div>
        </div>


    }

    @using (Html.BeginForm("DeleteSale", "Sales", FormMethod.Post, new { @class = "ui form root", role = "form" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index", "Users")
        </div>
    }
</div>
