@model MEGAPos.Models.U_O_M

@{
    ViewBag.Title = "DeleteUnit";
}

<h2>Delete Unit</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>U_O_M</h4>
    <hr />
    @using (Html.BeginForm(null, null, new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "ui form root form-white form-center", role = "form" }))
    {

        <div class="registers">
            <div class="ui buttons ">
                <a class="ui button" href="#">@Model.Unit_Name</a>
            </div>
        </div>


    }

    @using (Html.BeginForm("DeleteUnit", "Inventory", FormMethod.Post, new { @class = "ui form root", role = "form" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index","Users")
        </div>
    }
</div>
